var margin       = 10;				// margem de ajuste do desenho
var padding      = 20;				// espaçamento entre elemento radio e descrição
var maximumNodes = 7;				// quantidade máxima de nodos permitidos por fila
var boxSize      = 350;				// tamanho do elemento de contenção dos inputs de desenhos
var nodes        = new Array();		// variável principal que contém todos os valores que fazer referência ao nodos
var errorMargin  = 7;				// erro de aproximação gráfica das linhas que ligam os nodo
